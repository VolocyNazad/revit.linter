<Window
    x:Class="Revit.Linter.DialogPresenter.Views.ConfirmationDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:viewModels="clr-namespace:Revit.Linter.DialogPresenter.ViewModels"
    MinWidth="250"
    MinHeight="175"
    MaxWidth="500"
    MaxHeight="500"
    d:DataContext="{d:DesignInstance Type={x:Type viewModels:ConfirmationDialogViewModel}}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme
                    BaseTheme="Light"
                    PrimaryColor="Cyan"
                    SecondaryColor="DeepOrange" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate DataType="{x:Type system:String}">
                <TextBlock Text="{Binding}" TextWrapping="Wrap" />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <materialDesign:ColorZone
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        HorizontalContentAlignment="Stretch"
        VerticalContentAlignment="Stretch">
        <Grid Margin="24,20,24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ContentControl
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="{Binding Content}" />
            <StackPanel
                Grid.Row="1"
                Margin="0,16,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    MinWidth="80"
                    d:Content="Закрыть"
                    Content="{Binding CloseButtonText}"
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="CloseButton_Click" />
                <Button
                    MinWidth="80"
                    Margin="8,0,0,0"
                    d:Content="Повторить"
                    Content="{Binding ConfirmButtonText}"
                    IsDefault="True"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="ConfirmButton_Click" />
            </StackPanel>
        </Grid>
    </materialDesign:ColorZone>
</Window>
