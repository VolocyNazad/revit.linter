<UserControl
    x:Class="Revit.Linter.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:listPresenter="clr-namespace:Revit.Linter.DiagnosticListPresenter.Views;assembly=Revit.Linter.DiagnosticListPresenter"
    xmlns:local="clr-namespace:Revit.Linter.Views"
    xmlns:markupExtensions="clr-namespace:Revit.Linter.Infrastructure.MarkupExtensions"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reportPresenter="clr-namespace:Revit.Linter.DiagnosticReportPresenter.Views;assembly=Revit.Linter.DiagnosticReportPresenter"
    d:DesignHeight="600"
    d:DesignWidth="1600"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme
                    BaseTheme="Light"
                    PrimaryColor="Cyan"
                    SecondaryColor="DeepOrange" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="600" />
        </Grid.ColumnDefinitions>
        <ContentControl d:Content="{d:DesignInstance Type={x:Type reportPresenter:DiagnosticReportView}}" Content="{markupExtensions:TypeFromDI {x:Type reportPresenter:DiagnosticReportView}}" />
        <Rectangle
            Width="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Fill="{DynamicResource MaterialDesignDivider}" />
        <ContentControl
            Grid.Column="1"
            d:Content="{d:DesignInstance Type={x:Type listPresenter:DiagnosticListView}}"
            Content="{markupExtensions:TypeFromDI {x:Type listPresenter:DiagnosticListView}}" />
    </Grid>
</UserControl>
