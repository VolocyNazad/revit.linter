<UserControl
    x:Class="Revit.Linter.DiagnosticReportPresenter.Views.DiagnosticReportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Revit.Linter.DiagnosticReportPresenter.Infrastructure.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:infrastructure="clr-namespace:Revit.Linter.DiagnosticReportPresenter.Infrastructure"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Revit.Linter.DiagnosticReportPresenter.ViewModels"
    d:DataContext="{d:DesignInstance Type={x:Type viewModels:DiagnosticReportViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="1400"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme
                    BaseTheme="Light"
                    PrimaryColor="Cyan"
                    SecondaryColor="DeepOrange" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:DebugConverter x:Key="DebugConverter" />
            <viewModels:ServiceLocator x:Key="ServiceLocator" />
        </ResourceDictionary>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding InitializeCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <materialDesign:ColorZone
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        HorizontalContentAlignment="Stretch"
        VerticalContentAlignment="Stretch">
        <DockPanel>
            <DockPanel Height="45" DockPanel.Dock="Top">
                <ContentControl Content="{Binding RunDiagnosticView, Source={StaticResource ServiceLocator}}" />
                <Rectangle
                    Width="1"
                    Margin="0,5"
                    VerticalAlignment="Stretch"
                    DockPanel.Dock="Left"
                    Fill="{DynamicResource MaterialDesignDivider}" />
                <ComboBox
                    MinWidth="150"
                    Margin="5"
                    materialDesign:HintAssist.Hint="Document"
                    DisplayMemberPath="DisplayName"
                    ItemsSource="{Binding OpenedDocumentsViewModel.Collection, Source={StaticResource ServiceLocator}}"
                    SelectedValue="{Binding TargetDocumentTitle}"
                    SelectedValuePath="Title"
                    Style="{StaticResource MaterialDesignComboBox}" />
                <Rectangle
                    Width="1"
                    Margin="0,5"
                    VerticalAlignment="Stretch"
                    DockPanel.Dock="Left"
                    Fill="{DynamicResource MaterialDesignDivider}" />
                <ListBox
                    VerticalAlignment="Center"
                    d:ItemsSource="{d:SampleData ItemCount=2}"
                    DataContext="{Binding SeverityFilters}"
                    DockPanel.Dock="Left"
                    ItemsSource="{Binding}"
                    Style="{StaticResource MaterialDesignFilterChipSecondaryListBox}">
                    <ListBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource MaterialDesignFilterChipSecondaryListBoxItem}" TargetType="{x:Type ListBoxItem}">
                            <Setter Property="IsSelected" Value="{Binding IsActive, Mode=TwoWay}" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
                <ListBox
                    VerticalAlignment="Center"
                    d:ItemsSource="{d:SampleData ItemCount=3}"
                    DataContext="{Binding Filters}"
                    DockPanel.Dock="Left"
                    ItemsSource="{Binding}"
                    Style="{StaticResource MaterialDesignFilterChipSecondaryListBox}">
                    <ListBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource MaterialDesignFilterChipSecondaryListBoxItem}" TargetType="{x:Type ListBoxItem}">
                            <Setter Property="IsSelected" Value="{Binding IsActive, Mode=TwoWay}" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
                <Rectangle
                    Width="1"
                    Margin="0,5"
                    VerticalAlignment="Stretch"
                    DockPanel.Dock="Left"
                    Fill="{DynamicResource MaterialDesignDivider}" />
                <Button
                    Width="35"
                    Height="35"
                    Margin="5"
                    d:Command="{x:Null}"
                    Command="{Binding ExportCommand}"
                    Content="{materialDesign:PackIcon Kind=Export,
                                                      Size=30}"
                    DockPanel.Dock="Left"
                    IsEnabled="False"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="Export report" />
                <Rectangle
                    Width="1"
                    Margin="0,5"
                    VerticalAlignment="Stretch"
                    DockPanel.Dock="Left"
                    Fill="{DynamicResource MaterialDesignDivider}" />
                <Button
                    Content="{materialDesign:PackIcon Kind=FilterPlus,
                                                      Size=20}"
                    DockPanel.Dock="Right"
                    IsEnabled="False"
                    Style="{StaticResource MaterialDesignIconButton}" />
                <TextBox
                    Width="200"
                    Margin="5"
                    d:Text="Search text"
                    materialDesign:HintAssist.Hint="Search"
                    materialDesign:TextFieldAssist.HasLeadingIcon="True"
                    materialDesign:TextFieldAssist.LeadingIcon="Search"
                    DockPanel.Dock="Right"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                    Text="{Binding SearchField, UpdateSourceTrigger=LostFocus}" />
                <TextBlock
                    Margin="5"
                    VerticalAlignment="Center"
                    DockPanel.Dock="Right"
                    Text="Reports:" />
                <UIElement />
            </DockPanel>
            <Rectangle
                Height="1"
                HorizontalAlignment="Stretch"
                DockPanel.Dock="Top"
                Fill="{DynamicResource MaterialDesignDivider}" />
            <DataGrid
                d:ItemsSource="{d:SampleData ItemCount=2}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                ItemsSource="{Binding CollectionViewSource.View}">
                <DataGrid.RowStyle>
                    <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}" ItemsSource="{Binding Fixes}">
                                    <ContextMenu.ItemContainerStyle>
                                        <Style BasedOn="{StaticResource MaterialDesignMenuItem}" TargetType="MenuItem">
                                            <Setter Property="Header" Value="{Binding Title}" />
                                            <Setter Property="Command" Value="{Binding FixCommand}" />
                                            <Setter Property="Icon" Value="{Binding Icon}" />
                                        </Style>
                                    </ContextMenu.ItemContainerStyle>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Resources>
                    <infrastructure:BindingProxy x:Key="proxy" DataContext="{Binding}" />
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <DataTemplate.Resources>
                                    <Style x:Key="HasFixesPackIconStyle" TargetType="{x:Type materialDesign:PackIcon}">
                                        <Setter Property="Foreground" Value="#FFB74D" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Fixes}" Value="{x:Null}">
                                                <Setter Property="Foreground" Value="Transparent" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataTemplate.Resources>
                                <materialDesign:PackIcon
                                    Width="12"
                                    Height="12"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Kind="Idea"
                                    Style="{StaticResource HasFixesPackIconStyle}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Width="25"
                                    Height="25"
                                    Content="{materialDesign:PackIcon Kind=Refresh,
                                                                      Size=20}"
                                    IsEnabled="False"
                                    Style="{StaticResource MaterialDesignIconButton}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Severity" SortMemberPath="Severity">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <DataTemplate.Resources>
                                    <Style
                                        x:Key="SeverityChipStyle"
                                        BasedOn="{StaticResource MaterialDesignOutlineChip}"
                                        TargetType="{x:Type materialDesign:Chip}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                                <Setter Property="BorderBrush" Value="#FFB74D" />
                                                <Setter Property="Foreground" Value="#FFB74D" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Error">
                                                <Setter Property="BorderBrush" Value="#EF5350" />
                                                <Setter Property="Foreground" Value="#EF5350" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataTemplate.Resources>
                                <materialDesign:Chip
                                    Margin="0"
                                    d:Content="Severity"
                                    Content="{Binding Severity}"
                                    Style="{StaticResource SeverityChipStyle}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Code" SortMemberPath="Code">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox
                                    VerticalAlignment="Center"
                                    BorderThickness="0"
                                    IsReadOnly="True"
                                    IsReadOnlyCaretVisible="False">
                                    <TextBox.Style>
                                        <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="TextBox">
                                            <Setter Property="Text" Value="{Binding Code}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsObsolete}" Value="True">
                                                    <Setter Property="TextDecorations" Value="Strikethrough" />
                                                    <Setter Property="ToolTip" Value="{Binding ObsoleteDescription}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Message" SortMemberPath="Message">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <FlowDocumentScrollViewer
                                    Width="1800"
                                    Document="{Binding Message}"
                                    HorizontalScrollBarVisibility="Disabled"
                                    IsSelectionEnabled="True"
                                    VerticalScrollBarVisibility="Disabled" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Document" SortMemberPath="DocumentTitle">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox
                                    VerticalAlignment="Center"
                                    BorderThickness="0"
                                    IsReadOnly="True"
                                    IsReadOnlyCaretVisible="False"
                                    Style="{StaticResource MaterialDesignTextBox}"
                                    Text="{Binding DocumentTitle}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Created" SortMemberPath="Created">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox
                                    VerticalAlignment="Center"
                                    BorderThickness="0"
                                    IsReadOnly="True"
                                    IsReadOnlyCaretVisible="False"
                                    Style="{StaticResource MaterialDesignTextBox}"
                                    Text="{Binding Created}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </materialDesign:ColorZone>
</UserControl>
